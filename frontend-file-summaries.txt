{
  "frontend/src/pages/ProfilePage.tsx": "User profile page with all existing sections (stats, learning progress, predictions, portfolio) plus a new \"Security & Authentication\" section (visible only to authenticated users) containing a password setup button that opens `PasswordSetupModal` and the `BiometricAuthButton` component; also shows a \"Password Protected\" badge when a password hash is set.",
  "frontend/src/components/CreatePostModal.tsx": "Modal dialog for creating community forum posts with a textarea (max 500 chars), stock symbol tagging (up to 5 tags), character counter, and submit/cancel actions.",
  "frontend/src/pages/LearningModulePage.tsx": "Learning module index page showing all topic cards in a grid with completion status, progress bar overview, and navigation using TanStack Router `params` prop for type-safe dynamic routes.",
  "frontend/src/pages/TopicLessonPage.tsx": "Individual topic lesson page rendering markdown-like content with inline bold/headers, showing completion status, and linking to the quiz using type-safe `params` prop navigation.",
  "frontend/src/pages/TopicQuizPage.tsx": "Interactive quiz page with per-question answer selection, immediate feedback with explanations, score results screen with retake functionality; fixes missing `ArrowRight` import from `lucide-react`.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; adds the new `useGetSummaryStatistics` hook that calls `actor.getSummaryStatistics()` alongside all existing hooks for user profile, password, learning, predictions, portfolio, conversation, forum posts, news articles, and market alerts.",
  "frontend/src/components/MarketAlertBanner.tsx": "New glassmorphism-styled alert banner component that renders dismissible market alert cards with severity-coded colors (red=critical, amber=high, teal=medium), animated pulse dots for urgent alerts, stock symbol badges, relative timestamps, and a \"dismiss all\" option; returns null when no active alerts exist.",
  "frontend/src/pages/NewsModulePage.tsx": "Enhanced Financial News Intelligence Module with ML sentiment analytics, drastic event alert system, sentiment/impact charts, symbol radar, scatter plot, and tabbed feed/analytics layout; fixes imports to use `useMarketAlerts` (correct name) and `useGetSummaryStatistics` (new hook), and passes all required props (`open`, `nextId`) to `UploadNewsModal`.",
  "frontend/src/components/TopNav.tsx": "Sticky navigation bar with logo, nav links, bell alert badge, profile link, and login button; fixes the `alerts` possibly-undefined TypeScript error by using `alerts ?? []` before calling `.filter()`.",
  "frontend/src/components/PasswordSetupModal.tsx": "Modal dialog for setting or updating an account password; renders two password fields with show/hide toggles, a strength indicator, client-side SHA-256 hashing via Web Crypto API, inline validation, and a success confirmation screen — styled with glassmorphism consistent with the dark-finance theme.",
  "frontend/src/components/BiometricAuthButton.tsx": "WebAuthn-based biometric authentication component; fixes the `BufferSource` type error by changing `base64urlDecode` to explicitly allocate a plain `ArrayBuffer` and return `Uint8Array<ArrayBuffer>`, ensuring full compatibility with the WebAuthn `allowCredentials` API.",
  "frontend/src/pages/ApiReferencePage.tsx": "New API reference page documenting all four public backend query endpoints (getPublicNewsFeed, getPublicMarketAlerts, getPublicStockList, getPublicLeaderboard) with interactive \"Run Query\" buttons, collapsible return schemas, live JSON response display, and loading/error states — styled with teal/gold glass cards consistent with the platform theme.",
  "frontend/src/App.tsx": "Main app component with TanStack Router setup; adds the `/api` route mapped to the new ApiReferencePage alongside all existing routes (home, learn, playground, news, portfolio, advisor, community, profile).",
  "frontend/src/data/mockStocks.ts": "Mock stock data module with OHLCV history generation, AI prediction/outcome simulation, and a new `getLivePrice(symbol)` utility that applies a ±0.5% random walk per call using a persistent `Map` to simulate live price drift.",
  "frontend/src/hooks/useLivePrice.ts": "New React hook `useLivePrice(symbol, intervalMs?)` that polls `getLivePrice` on a configurable interval (default 3s), tracks session-open and previous-tick prices, and returns `{ price, previousPrice, sessionOpenPrice, changePercent, absoluteChange, isUp }` with proper cleanup on unmount.",
  "frontend/src/components/LivePriceTicker.tsx": "New component displaying a real-time price ticker for a given symbol using `useLivePrice`; supports full and compact modes, shows symbol, live price with a flash animation on each update, absolute change, and percentage change with green/red/yellow color coding and a LIVE pulse badge.",
  "frontend/src/components/LivePriceChart.tsx": "New Recharts-based rolling line chart component that maintains up to 60 price ticks (appended every 3s via `useLivePrice`), colors the line teal when at/above session-open and red when below, shows a gold dashed ReferenceLine at the session-open price, and uses a custom tooltip with elapsed time.",
  "frontend/src/pages/StockPlaygroundPage.tsx": "Stock prediction playground page updated to show a `LivePriceTicker` and `LivePriceChart` (labelled \"Live Price\") for the selected symbol above the existing historical chart (now labelled \"Historical Price\"), while preserving all prediction functionality.",
  "frontend/src/pages/PortfolioAnalyzerPage.tsx": "Comprehensive Portfolio Analyzer with tabbed interface (Overview, Holdings, Performance, Risk), live price integration, allocation/sector charts, 30-day performance simulation, and risk analysis; fixes the default import of `LivePriceTicker` and adds explicit `PerformancePoint` interface to resolve the `never[]` type inference error in `generatePerformanceData`.",
  "frontend/src/index.css": "Global styles with OKLCH design tokens; adds a `@keyframes price-flash` animation and `.price-flash` component class used by `LivePriceTicker` to visually indicate each price update.",
  "frontend/src/components/UploadNewsModal.tsx": "New modal dialog for admins to upload news articles; includes fields for title, summary, related stock symbols (up to 5, with add/remove), and market impact score (0–100); sentiment is auto-calculated by the backend, with inline validation, loading state, and a success confirmation screen.",
  "frontend/src/pages/AdvisorPage.tsx": "AI Financial Strategy Advisor chatbot with context-aware responses drawing from portfolio, news sentiment, and prediction history; features typing indicator, message history, quick prompts sidebar, and comprehensive financial Q&A covering portfolio, risk, strategy, macro, and earnings topics."
}